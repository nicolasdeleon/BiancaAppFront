class Contract {
    constructor(id, title, creationTime, status) {
        this.id = id,
        this.title = title,
        this.creationTime = creationTime,
        this.status = status
    }
}

export default Contract;